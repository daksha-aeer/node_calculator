# Node setup

```sh
npm init # initialize node project
npm install express #install express package for server
npm install nodemon #to install nodemon. nodemon updates server based on code changes
npm run dev #to run nodemon. this is a custom command
```